# Baseline mode — no YummyGraph, pure mini-swe-agent (control group)
agent:
  agent_class: 'eval.agents.yummygraph_agent.YummyGraphAgent'
  yummygraph_mode: 'baseline'
  step_limit: 30
  cost_limit: 3.0

environment:
  environment_class: 'docker'
